{{ form_start(form, {attr:{class:"form-inline"}}) }}
    {{ form_rest(form) }}
    <input type="submit" class="btn btn-default" value="{{ 'button.upload_file' | trans({}, 'translation-bundle') }}">
{{ form_end(form) }}
